{
  "buildCommand": "cd docs/arquitetura-solucao && npm install && npm run build && cd ../.. && mkdir -p dist && find docs -maxdepth 1 -type f -exec cp {} dist/ \\; && mkdir -p dist/arquitetura-solucao && cp -r docs/arquitetura-solucao/dist/. dist/arquitetura-solucao/",
  "outputDirectory": "dist",
  "installCommand": "",
  "framework": null,
  "rewrites": [
    { "source": "/rei-permutas", "destination": "/pitch-deck-rei-permutas.html" }
  ],
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        { "key": "Access-Control-Allow-Origin",  "value": "*" },
        { "key": "Access-Control-Allow-Methods", "value": "GET,POST,OPTIONS" },
        { "key": "Access-Control-Allow-Headers", "value": "Content-Type" }
      ]
    }
  ]
}
